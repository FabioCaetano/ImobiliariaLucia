"use client";
import { useMemo, useState } from "react";
import { useSearchParams } from "next/navigation";
import { PropertyCard } from "./PropertyCard";
import { demoProperties } from "../lib/demo-data";

export function PropertyListing() {
  const search = useSearchParams();
  const [filters, setFilters] = useState({ q: search.get("q") || "", purpose: search.get("purpose") || "", neighborhood: search.get("neighborhood") || "", type: search.get("type") || "", min: "", max: search.get("max") || "", bedrooms: search.get("bedrooms") || "", bathrooms: "", parking: "", area: "", furnished: false, pets: false, pool: false, elevator: false, balcony: false, air: false, gated: false });
  const [draft, setDraft] = useState(filters); const [view, setView] = useState<"grid"|"list">("grid"); const [sort, setSort] = useState("featured"); const [page, setPage] = useState(1);
  const results = useMemo(() => { let rows = [...demoProperties]; const f = filters; if (f.q) rows=rows.filter(p=>`${p.title} ${p.code} ${p.neighborhood}`.toLowerCase().includes(f.q.toLowerCase())); if(f.purpose)rows=rows.filter(p=>p.purpose===f.purpose);if(f.neighborhood)rows=rows.filter(p=>p.neighborhood===f.neighborhood);if(f.type)rows=rows.filter(p=>p.type===f.type);if(+f.min)rows=rows.filter(p=>p.price>=+f.min);if(+f.max)rows=rows.filter(p=>p.price<=+f.max);if(+f.bedrooms)rows=rows.filter(p=>p.bedrooms>=+f.bedrooms);if(+f.bathrooms)rows=rows.filter(p=>p.bathrooms>=+f.bathrooms);if(+f.parking)rows=rows.filter(p=>p.parking>=+f.parking);if(+f.area)rows=rows.filter(p=>p.area>=+f.area);if(f.furnished)rows=rows.filter(p=>p.furnished);if(f.pets)rows=rows.filter(p=>p.pets);if(f.pool)rows=rows.filter(p=>p.amenities.includes("Pool"));if(f.elevator)rows=rows.filter(p=>p.amenities.includes("Elevator"));if(f.balcony)rows=rows.filter(p=>p.amenities.includes("Balcony"));if(f.air)rows=rows.filter(p=>p.amenities.includes("Air conditioning")); if(sort==="price-asc")rows.sort((a,b)=>a.price-b.price);if(sort==="price-desc")rows.sort((a,b)=>b.price-a.price);if(sort==="recent")rows.sort((a,b)=>b.createdAt.localeCompare(a.createdAt));return rows; }, [filters, sort]);
  function set<K extends keyof typeof draft>(key: K, value: (typeof draft)[K]) { setDraft((v)=>({...v,[key]:value})); }
  function clear(){const empty={q:"",purpose:"",neighborhood:"",type:"",min:"",max:"",bedrooms:"",bathrooms:"",parking:"",area:"",furnished:false,pets:false,pool:false,elevator:false,balcony:false,air:false,gated:false};setDraft(empty);setFilters(empty);setPage(1)}
  return <div className="container listing-layout"><aside className="filter-panel"><h2>Filter properties</h2><label>Search<input value={draft.q} onChange={e=>set("q",e.target.value)} placeholder="Code, neighbourhood or keyword"/></label><label>Listing type<select value={draft.purpose} onChange={e=>set("purpose",e.target.value)}><option value="">All</option><option>Buy</option><option>Rent</option><option>Seasonal</option></select></label><label>City<select><option>Toronto</option></select></label><label>Neighbourhood<select value={draft.neighborhood} onChange={e=>set("neighborhood",e.target.value)}><option value="">All</option>{[...new Set(demoProperties.map(p=>p.neighborhood))].map(n=><option key={n}>{n}</option>)}</select></label><label>Property type<select value={draft.type} onChange={e=>set("type",e.target.value)}><option value="">All</option>{[...new Set(demoProperties.map(p=>p.type))].map(n=><option key={n}>{n}</option>)}</select></label><div className="filter-checks"><label>Min. price<input value={draft.min} onChange={e=>set("min",e.target.value)} type="number"/></label><label>Max. price<input value={draft.max} onChange={e=>set("max",e.target.value)} type="number"/></label><label>Bedrooms<input value={draft.bedrooms} onChange={e=>set("bedrooms",e.target.value)} type="number" min="0"/></label><label>Bathrooms<input value={draft.bathrooms} onChange={e=>set("bathrooms",e.target.value)} type="number" min="0"/></label><label>Parking<input value={draft.parking} onChange={e=>set("parking",e.target.value)} type="number" min="0"/></label><label>Min. area<input value={draft.area} onChange={e=>set("area",e.target.value)} type="number" min="0"/></label></div><div className="filter-checks">{[["furnished","Furnished"],["pets","Pet-friendly"],["pool","Pool"],["elevator","Elevator"],["balcony","Balcony"],["air","Air conditioning"],["gated","Gated community"]].map(([k,l])=><label key={k}><input type="checkbox" checked={Boolean(draft[k as keyof typeof draft])} onChange={e=>set(k as keyof typeof draft,e.target.checked as never)}/>{l}</label>)}</div><div className="filter-actions"><button className="btn btn-primary" onClick={()=>{setFilters(draft);setPage(1)}}>Apply filters</button><button className="btn" onClick={clear}>Clear</button></div></aside><section><div className="listing-toolbar"><h2>{results.length} properties found</h2><div className="toolbar-controls"><select value={sort} onChange={e=>setSort(e.target.value)} aria-label="Sort"><option value="featured">Featured</option><option value="recent">Newest</option><option value="price-asc">Lowest price</option><option value="price-desc">Highest price</option></select><button aria-label="Grid view" onClick={()=>setView("grid")}>▦</button><button aria-label="List view" onClick={()=>setView("list")}>☷</button></div></div>{results.length?<div className={`listing-grid ${view==="list"?"list-view":""}`}>{results.map(p=><PropertyCard key={p.id} property={p} list={view==="list"}/>)}</div>:<div className="empty-state"><h3>No properties found</h3><p>Try removing some filters to broaden your search.</p><button className="btn btn-primary" onClick={clear}>Clear filters</button></div>}<div className="pagination"><button className="active" onClick={()=>setPage(1)}>1</button><button onClick={()=>setPage(2)}>2</button><button onClick={()=>setPage(3)}>3</button><button onClick={()=>setPage(page+1)}>→</button></div></section></div>;
}
